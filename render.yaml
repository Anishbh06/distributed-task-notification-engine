# Render Blueprint - Multi-service deployment
services:
  - type: web
    name: task-engine-api
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    plan: free
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: task-engine-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          type: redis
          name: task-engine-redis
          property: connectionString
      - key: CELERY_BROKER_URL
        fromService:
          type: redis
          name: task-engine-redis
          property: connectionString
      - key: JWT_SECRET_KEY
        generateValue: true
      - key: PROGRESS_CHANNEL
        value: task:progress
    healthCheckPath: /health

  - type: web
    name: task-engine-worker
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    plan: free
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: task-engine-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          type: redis
          name: task-engine-redis
          property: connectionString
      - key: CELERY_BROKER_URL
        fromService:
          type: redis
          name: task-engine-redis
          property: connectionString
      - key: PROGRESS_CHANNEL        # ← was missing
        value: task:progress
      - key: JWT_SECRET_KEY          # ← was missing
        generateValue: true
    healthCheckPath: /health         # ← was missing, Render was killing the worker
    dockerCommand: sh /app/scripts/worker.sh

  - type: redis
    name: task-engine-redis
    plan: free
    ipAllowList: []

databases:
  - name: task-engine-db
    plan: free
    databaseName: task_engine